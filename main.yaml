- name: set up "{{ component }}" server
  become: yes
  #hosts: "{{ component }}"
  hosts: localhost
  connection: local
  
  roles:            # this will automatically load a folder called roles in current working directory.
  - "{{ component }}"

# {shift + tab} for correct indentation